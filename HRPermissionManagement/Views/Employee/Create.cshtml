@model HRPermissionManagement.Models.Employee

@{
    ViewData["Title"] = "Yeni Personel";
}

<div class="row mt-4">
    <div class="col-md-8 offset-md-2">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Yeni Personel Kaydı</h3>
            </div>
            <div class="card-body">
                <form asp-action="Create" method="post">

                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label>Ad</label>
                                <input type="text" asp-for="Name" class="form-control" required />
                            </div>
                            <div class="mb-3">
                                <label>Soyad</label>
                                <input type="text" asp-for="Surname" class="form-control" required />
                            </div>
                            <div class="mb-3">
                                <label>Departman</label>
                                <select asp-for="DepartmentId" class="form-control" required>
                                    <option value="">Seçiniz...</option>
                                    @{
                                        var depts = ViewBag.Departmanlar as List<HRPermissionManagement.Models.Department> ?? new List<HRPermissionManagement.Models.Department>();
                                    }
                                    @foreach (var dept in depts)
                                    {
                                        <option value="@dept.Id">@dept.Name</option>
                                    }
                                </select>
                            </div>
                            <div class="mb-3">
                                <label>İşe Giriş Tarihi</label>
                                <input type="date" asp-for="StartDate" class="form-control" value="@DateTime.Now.ToString("yyyy-MM-dd")" />
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="mb-3">
                                <label>E-Posta (Kullanıcı Adı)</label>
                                <input type="email" asp-for="Email" class="form-control" required />
                            </div>
                            <div class="mb-3">
                                <label>Şifre</label>
                                <input type="password" asp-for="Password" class="form-control" required />
                            </div>
                            <div class="mb-3">
                                <label>Yıllık İzin Hakkı</label>
                                <input type="number" asp-for="AnnualLeaveRight" class="form-control" value="14" />
                            </div>

                            <div class="mb-3 pt-4">
                                <div class="custom-control custom-switch">
                                    <input type="checkbox" class="custom-control-input" id="isAdminCheck" asp-for="IsAdmin">
                                    <label class="custom-control-label" for="isAdminCheck">Bu Kullanıcı Yönetici (Admin) mi?</label>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="d-flex justify-content-end mt-3">
                        <a href="/Employee/Index" class="btn btn-outline-secondary mr-2">İptal</a>
                        <button type="submit" class="btn btn-dark">
                            <i class="fas fa-save"></i> Personeli Kaydet
                        </button>
                    </div>

                </form>
            </div>
        </div>
    </div>
</div>